# stayhydated

Dioxus source for the `stayhydated.github.io` project index and shared Rust crates used by stayhydated project sites.

## Web

The index lives in `web` and is localized with `es-fluent` using locale assets in `web/assets/locales`.

```sh
cargo xtask build web
```
